1. static_pres.db 	-> Building geometry
2. static_p_V2.db 	-> Define elements, mesh and join different formulations with conta/targe
3. static_p_V3.db 	-> Change element size from 5 to 10 mm instead of scaling down the model to halven the number of elements. Progress with BC